import React, { Fragment } from 'react'
import ReactDOM from 'react-dom/client'

import './../css/admin.css'

const admin = ReactDOM.createRoot(document.getElementById('admin'))
admin.render(
  <Fragment>
    <h1>Hallo from Admin UI page!</h1>
  </Fragment>
)
